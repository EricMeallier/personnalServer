---

- name: Copy unsecured nginx configuration file
  copy:
    src: gogsUnsecured.conf
    dest: /opt/nginx/conf/server/gogsUnsecured.conf

- name: Nginx service reload/start
  systemd:
    name: 'nginx'
    daemon_reload: true
    state: started
    enabled: true
