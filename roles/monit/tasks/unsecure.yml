---

- name: Copy unsecured nginx configuration file
  copy:
    src: monitUnsecured.conf
    dest: /opt/nginx/conf/server/monitUnsecured.conf

- name: Nginx service reload/start
  systemd:
    name: 'nginx'
    daemon_reload: true
    state: started
    enabled: true
