---

- name: Copy unsecured nginx configuration file
  copy:
    src: ethercalcUnsecured.conf
    dest: /opt/nginx/conf/server/ethercalcUnsecured.conf

- name: Nginx service reload/start
  systemd:
    name: 'nginx'
    daemon_reload: true
    state: started
    enabled: true
