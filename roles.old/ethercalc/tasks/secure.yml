---

- name: generate ethercalc certificate
  shell: /usr/bin/certbot certonly -d ethercalc.meallier.fr -m eric@meallier.fr --agree-tos --webroot --no-eff-email --webroot-path /opt/nginx/html  --keep-until-expiring --non-interactive  

# finish installation with ssl termination
- name: Copy full nginx ssl configuration file
  copy:
    src: ethercalcSSL.conf
    dest: /opt/nginx/conf/server/ethercalcSSL.conf

- name: Nginx service restart with full configuration
  systemd:
    name: 'nginx'
    state: restarted
